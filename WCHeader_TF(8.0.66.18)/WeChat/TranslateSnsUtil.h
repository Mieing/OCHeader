@interface TranslateSnsUtil : MMObject

+ (id)getUserTranslateRootDir;
+ (BOOL)isTranslateSnsOff;
+ (BOOL)couldTranslateSnsContent:(id)a0;
+ (BOOL)couldTranslateSnsComment:(id)a0;
+ (BOOL)couldTranslateSns;

@end
